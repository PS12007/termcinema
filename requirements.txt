# Core (required)
numpy>=1.24
opencv-python>=4.8

# Optional: YouTube / arbitrary URL playback
yt-dlp>=2024.1.1

# Optional: CPU/memory stats in the status bar
psutil>=5.9

# System requirement (not pip-installable): FFmpeg must be on PATH
# for audio playback (ffplay) and for OpenCV's video backend.
#   macOS:   brew install ffmpeg
#   Ubuntu:  sudo apt install ffmpeg
#   Windows: https://ffmpeg.org/download.html (add to PATH)
